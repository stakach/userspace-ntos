[package]
name = "nt-power-manager"
description = "Core NT Power Manager: per-devnode power transitions plus per-thread execution-state assertions, pulses, aggregate accounting, and rundown. no_std + alloc."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
nt-power-types = { workspace = true }

[lints.clippy]
undocumented_unsafe_blocks = "deny"
