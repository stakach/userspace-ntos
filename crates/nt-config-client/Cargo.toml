[package]
name = "nt-config-client"
description = "Ergonomic client for the NT Configuration Manager service: path-addressed registry operations and bounded semantic devnode property queries over a pluggable backend. no_std + alloc."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
nt-config-abi = { workspace = true }

[dev-dependencies]
nt-config-manager = { workspace = true }
nt-config-server = { workspace = true }

[lints.clippy]
undocumented_unsafe_blocks = "deny"
