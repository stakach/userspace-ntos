[package]
name = "nt-lpc-continuation"
description = "Generation-exact kernel continuation state for blocking NT LPC services (no_std + alloc, host-testable)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[features]
default = []
std = []

[lints.clippy]
undocumented_unsafe_blocks = "deny"
