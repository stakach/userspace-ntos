[package]
name = "nt-resource-manager"
description = "Canonical NT resource assignment store: device→resource fixtures, ownership validation, MMIO mapping lifetimes, and interrupt assignments, with stale-ID rejection. no_std + alloc; the authority behind the HAL service."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
nt-hal-abi = { workspace = true }

[lints.clippy]
undocumented_unsafe_blocks = "deny"
