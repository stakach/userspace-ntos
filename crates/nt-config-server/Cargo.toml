[package]
name = "nt-config-server"
description = "Transport-agnostic NT Configuration Manager service dispatcher for path-addressed registry operations and bounded semantic devnode property queries. no_std + alloc."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
nt-config-abi = { workspace = true }
nt-config-manager = { workspace = true }
nt-status = { workspace = true }

[lints.clippy]
undocumented_unsafe_blocks = "deny"
